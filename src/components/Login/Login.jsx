import styled from "styled-components";

const Login = () => {
  return(
    <LoginContainer>로그인화면임</LoginContainer>
  )
};

export default Login;

export const LoginContainer = styled.div`
  margin-top: 15rem;
`