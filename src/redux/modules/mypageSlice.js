import { createSlice, createAsyncThunk } from "@reduxjs/toolkit";
import { apis } from "./APi/apis";
import { getCookieToken } from "../../utils/cookie";

// export const __getCompanyInfo = createSlice(
//   "companyInfo",
//   async (payload, thunkAPI)
// )
